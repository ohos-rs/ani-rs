// Auto-generated arkvm smoke test for ani-example-new-basic.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

native function add(a: int, b: int): int;
native function divide(a: int, b: int): int;
native function factorial(n: int): long;
native function fibonacci(n: int): long;
native function gcd(a: int, b: int): int;
native function greet(name: string): string;
native function is_prime(n: int): boolean;
native function multiply(a: int, b: int): int;
native function string_length(s: string): int;
native function subtract(a: int, b: int): int;



loadLibrary("ani_example_new_basic");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-new-basic");
__assert_eq_int("add", add(2, 3), 5);
__assert_eq_int("subtract", subtract(9, 4), 5);
__assert_eq_int("multiply", multiply(6, 7), 42);
__assert_eq_int("divide", divide(8, 2), 4);
__assert_eq_string("greet", greet("ArkTS"), "Hello, ArkTS!");
__assert_eq_int("string_length", string_length("hello"), 5);
__assert_eq_long("factorial", factorial(5), 120);
__assert_true("is_prime_17", is_prime(17));

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-new-basic");
