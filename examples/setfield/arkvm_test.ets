// Auto-generated arkvm smoke test for ani-example-setfield.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function create_person_data(name: string, age: int, height: double): long;
export native function destroy_person_data(ptr: long): void;
export native function get_age(person: Object): int;
export native function get_field_by_handle(target: Object, field: Object): int;
export native function get_price(product: Object): double;
export native function person_data_get_age(ptr: long): int;
export native function person_data_get_height(ptr: long): double;
export native function person_data_get_name(ptr: long): string;
export native function person_data_set_age(ptr: long, age: int): void;
export native function person_data_set_height(ptr: long, height: double): void;
export native function person_data_set_name(ptr: long, name: string): void;
export native function set_age(person: Object, age: int): void;
export native function set_field_by_handle(target: Object, field: Object, value: int): void;
export native function set_price(product: Object, price: double): void;



loadLibrary("ani_example_setfield");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-setfield");
let personData = create_person_data("bob", 20, 1.80);
__assert_true("create_person_data_handle", personData > 0);
__assert_eq_int("person_data_get_age", person_data_get_age(personData), 20);
person_data_set_age(personData, 21);
__assert_eq_int("person_data_set_age", person_data_get_age(personData), 21);
__assert_eq_string("person_data_get_name", person_data_get_name(personData), "bob");
destroy_person_data(personData);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-setfield");
