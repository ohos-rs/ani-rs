// Auto-generated arkvm smoke test for ani-example-impl-block.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export class Widget {
  private _count: int = 0;
  private _name: string = "";
  native constructor(name: string, count: int);
  static native __ani_native_revision(): int;
  static get revision(): int {
    return Widget.__ani_native_revision();
  }
  static native __ani_native_set_revision(value: int): void;
  static set revision(value: int) {
    Widget.__ani_native_set_revision(value);
  }
  native __ani_native_get_count(): int;
  get count(): int {
    return this.__ani_native_get_count();
  }
  native __ani_native_set_count(count: int): void;
  set count(count: int) {
    this.__ani_native_set_count(count);
  }
  native __ani_native_get_name(): string;
  get name(): string {
    return this.__ani_native_get_name();
  }
  static native sum(a: int, b: int): int;
  native $_get(index: double): string;
  native $_iterator(): WidgetIndexIterator;
  native $_set(index: double, value: boolean): void;
  native $_set(index: double, value: string): void;
  native bump(delta: int): int;
  native checked_snapshot(min_count: int): WidgetSnapshot;
  native choose_snapshot(detailed: boolean): WidgetSnapshot | string;
  native consume(): string;
  native describe(): string;
  native __ani_native_maybe_checked_snapshot(include: boolean, min_count: int): WidgetSnapshot | null;
  maybe_checked_snapshot(include: boolean, min_count: int): WidgetSnapshot | null | undefined {
    let __ani_result = this.__ani_native_maybe_checked_snapshot(include, min_count);
    return __ani_result == null ? undefined : __ani_result;
  }
  native __ani_native_maybe_name(include: boolean): string | null;
  maybe_name(include: boolean): string | null | undefined {
    let __ani_result = this.__ani_native_maybe_name(include);
    return __ani_result == null ? undefined : __ani_result;
  }
  native __ani_native_maybe_snapshot(include: boolean): WidgetSnapshot | null;
  maybe_snapshot(include: boolean): WidgetSnapshot | null | undefined {
    let __ani_result = this.__ani_native_maybe_snapshot(include);
    return __ani_result == null ? undefined : __ani_result;
  }
  native merge_snapshot_input(value: WidgetSnapshot | string): string;
  native __ani_native_rename(name: string | null): string | null;
  rename(name: string | null | undefined): string | null | undefined {
    let __ani_result = this.__ani_native_rename(name == undefined ? null : name);
    return __ani_result == null ? undefined : __ani_result;
  }
  native snapshot(): WidgetSnapshot;
}

export class WidgetIndexIterator implements Iterator<int> {
  current: int = 0;
  end: int = 0;
  native __ani_native_next(): int | null;
  next(): IteratorResult<int> {
    let __ani_result = this.__ani_native_next();
    return {
      done: __ani_result == null,
      value: __ani_result == null ? undefined : __ani_result
    };
  }
}

export class WidgetSnapshot {
  label: string = "";
  total: int = 0;
}

export native function resolve_widget_special_methods(class_name: string): boolean;



loadLibrary("ani_example_impl_block");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-impl-block");
let widget = new Widget("impl", 2);
__assert_eq_string("Widget.name", widget.name, "impl");
__assert_eq_int("Widget.count", widget.count, 2);
__assert_eq_string("Widget.maybe_name_some", widget.maybe_name(true) as String, "impl");
__assert_true("Widget.maybe_name_none", widget.maybe_name(false) == undefined);
let widgetSnapshot = widget.snapshot();
__assert_eq_string("Widget.snapshot_label", widgetSnapshot.label, "impl");
__assert_eq_int("Widget.snapshot_total", widgetSnapshot.total, 2);
let maybeSnapshot = widget.maybe_snapshot(true) as WidgetSnapshot;
__assert_eq_string("Widget.maybe_snapshot_label", maybeSnapshot.label, "impl");
__assert_true("Widget.maybe_snapshot_none", widget.maybe_snapshot(false) == undefined);
let checkedSnapshot = widget.checked_snapshot(2);
__assert_eq_int("Widget.checked_snapshot_total", checkedSnapshot.total, 2);
__assert_throws("Widget.checked_snapshot_error", (): void => {
  widget.checked_snapshot(3);
});
let maybeCheckedSnapshot = widget.maybe_checked_snapshot(true, 2) as WidgetSnapshot;
__assert_eq_int("Widget.maybe_checked_snapshot_total", maybeCheckedSnapshot.total, 2);
__assert_true("Widget.maybe_checked_snapshot_none", widget.maybe_checked_snapshot(false, 2) == undefined);
__assert_throws("Widget.maybe_checked_snapshot_error", (): void => {
  widget.maybe_checked_snapshot(true, 3);
});
let chooseSnapshot = widget.choose_snapshot(true) as WidgetSnapshot;
__assert_eq_string("Widget.choose_snapshot_label", chooseSnapshot.label, "impl");
let chooseText = widget.choose_snapshot(false) as String;
__assert_eq_string("Widget.choose_snapshot_text", chooseText.toString(), "Widget(impl, 2)");
let mergeInput = new WidgetSnapshot();
mergeInput.label = "child";
mergeInput.total = 5;
__assert_eq_string("Widget.merge_snapshot_input_object", widget.merge_snapshot_input(mergeInput), "impl:child+7");
__assert_eq_string("Widget.merge_snapshot_input_string", widget.merge_snapshot_input("plain"), "impl:plain");
let previousName = widget.rename("renamed");
__assert_eq_string("Widget.rename_previous", previousName as String, "impl");
__assert_eq_string("Widget.name_after_rename", widget.name, "renamed");
__assert_throws("Widget.rename_undefined", (): void => {
  widget.rename(undefined);
});
widget.count = 5;
__assert_eq_int("Widget.count_after_set", widget.count, 5);
__assert_eq_int("Widget.bump", widget.bump(3), 8);
__assert_eq_int("Widget.count_after_bump", widget.count, 8);
__assert_eq_string("Widget.describe", widget.describe(), "Widget(renamed, 8)");
__assert_eq_string("Widget.consume", widget.consume(), "renamed#8");
__assert_eq_string("Widget.index_get", widget.$_get(4.0), "renamed#4");
widget.$_set(2.0, "slot");
__assert_eq_string("Widget.index_set_text_name", widget.name, "slot@2");
widget.$_set(6.0, true);
__assert_eq_int("Widget.index_set_flag_true", widget.count, 6);
widget.$_set(3.0, false);
__assert_eq_int("Widget.index_set_flag_false", widget.count, -3);
let widgetIter = widget.$_iterator();
let widgetIterFirst = widgetIter.next();
__assert_true("Widget.iterator_negative_done", widgetIterFirst.done);
__assert_true("Widget.iterator_negative_value", widgetIterFirst.value == undefined);
let widgetIterEnd = widgetIter.next();
__assert_true("Widget.iterator_negative_repeat_done", widgetIterEnd.done);
__assert_true("Widget.iterator_negative_repeat_value", widgetIterEnd.value == undefined);
widget.count = 4;
let widgetIterPositive = widget.$_iterator();
let widgetIterPositiveFirst = widgetIterPositive.next();
__assert_true("Widget.iterator_positive_first_done", !widgetIterPositiveFirst.done);
__assert_eq_int("Widget.iterator_positive_first_value", widgetIterPositiveFirst.value as int, 0);
let widgetIterPositiveSecond = widgetIterPositive.next();
__assert_eq_int("Widget.iterator_positive_second_value", widgetIterPositiveSecond.value as int, 1);
let widgetIterPositiveThird = widgetIterPositive.next();
__assert_eq_int("Widget.iterator_positive_third_value", widgetIterPositiveThird.value as int, 2);
let widgetIterPositiveFourth = widgetIterPositive.next();
__assert_eq_int("Widget.iterator_positive_fourth_value", widgetIterPositiveFourth.value as int, 3);
let widgetIterPositiveEnd = widgetIterPositive.next();
__assert_true("Widget.iterator_positive_end_done", widgetIterPositiveEnd.done);
__assert_true("Widget.iterator_positive_end_value", widgetIterPositiveEnd.value == undefined);
__assert_eq_int("Widget.sum", Widget.sum(2, 4), 6);
__assert_eq_int("Widget.revision_initial", Widget.revision, 1);
Widget.revision = 7;
__assert_eq_int("Widget.revision_after_set", Widget.revision, 7);
__assert_true(
  "Widget.resolve_special_methods",
  resolve_widget_special_methods("arkvm_test.Widget")
);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-impl-block");
