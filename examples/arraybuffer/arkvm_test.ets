// Auto-generated arkvm smoke test for ani-example-arraybuffer.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function buffer_length(buf: ArrayBuffer): int;
export native function concat_buffers(a: ArrayBuffer, b: ArrayBuffer): ArrayBuffer;
export native function create_buffer(size: int): ArrayBuffer;
export native function first_byte(buffer: ArrayBuffer): short;
export native function make_clamped_array(): Uint8ClampedArray;
export native function make_data_view(): DataView;
export native function make_u16_array(): Uint16Array;
export native function process_buffer(buf: ArrayBuffer): int;
export native function replace_first_byte(buffer: ArrayBuffer, value: short): ArrayBuffer;
export native function replace_first_u16(values: Uint16Array): Uint16Array;
export native function sum_bytes(buf: ArrayBuffer): long;
export native function sum_clamped_array(values: Uint8ClampedArray): long;
export native function sum_data_view(view: DataView): long;
export native function sum_u16_array(values: Uint16Array): long;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
promise.then(
(value: T): void => { __ani_rs_promise_resolve(token, value as Object); },
(reason: Object): void => { __ani_rs_promise_reject(token, reason); }
);
}
export native function __ani_rs_cancel_runtime_task(token: long, reason: Object): void;
export class AniCancelHandle {
private token: long;
constructor(token: long) { this.token = token; }
cancel(reason: Object): void { __ani_rs_cancel_runtime_task(this.token, reason); }
}



loadLibrary("ani_example_arraybuffer");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-arraybuffer");
let bufA = create_buffer(8);
let bufB = create_buffer(4);
__assert_eq_int("buffer_length", buffer_length(bufA), 8);
let merged = concat_buffers(bufA, bufB);
__assert_eq_int("concat_buffers_length", buffer_length(merged), 12);
__assert_true("process_buffer_non_negative", process_buffer(merged) >= 0);
let mutated = replace_first_byte(bufA, 255 as short);
__assert_eq_int("arraybuffer_copy_on_write", first_byte(mutated), 255);
let typed = make_u16_array();
__assert_eq_long("typed_array_roundtrip", sum_u16_array(typed), 65794);
let typedInput = new Uint16Array(3);
typedInput[0] = 4;
typedInput[1] = 5;
typedInput[2] = 6;
__assert_eq_long("typed_array_native_input", sum_u16_array(typedInput), 15);
let changedTyped = replace_first_u16(typedInput);
__assert_eq_long("typed_array_cow_result", sum_u16_array(changedTyped), 110);
__assert_eq_long("typed_array_cow_input_unchanged", sum_u16_array(typedInput), 15);
let clamped = make_clamped_array();
__assert_eq_long("uint8_clamped_roundtrip", sum_clamped_array(clamped), 383);
let view = make_data_view();
__assert_eq_long("data_view_range", sum_data_view(view), 5);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-arraybuffer");
