// Auto-generated arkvm smoke test for ani-example-new-class.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

class Calculator {
  native add(a: int, b: int): int;
  native divide(a: double, b: double): double;
  native multiply(a: double, b: double): double;
  native subtract(a: int, b: int): int;
  static native create(): long;
}

class Person {
  native constructor(name: string, age: int);
  native destroy(): void;
  native greet(): string;
  native person_get_age(): int;
  get age(): int {
    return this.person_get_age();
  }
  native person_get_name(): string;
  get name(): string {
    return this.person_get_name();
  }
  native person_set_age(age: int): void;
  set age(age: int) {
    this.person_set_age(age);
  }
}



loadLibrary("ani_example_new_class");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-new-class");
let calcToken = Calculator.create();
__assert_true("Calculator.create_handle", calcToken > 0);

let person = new Person("Alice", 30);
__assert_eq_string("Person.name", person.name, "Alice");
__assert_eq_int("Person.age", person.age, 30);
person.age = 31;
__assert_eq_int("Person.age_after_set", person.age, 31);
__assert_eq_string("Person.greet", person.greet(), "Hello, I'm Alice and I'm 31 years old!");
person.destroy();

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-new-class");
