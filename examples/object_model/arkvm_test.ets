// Auto-generated arkvm smoke test for ani-example-object-model.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

class UserProfile {
  id: int = 0;
  name: string = "";
  active: boolean = false;
}

native function __ani_native_describe_optional_user_profile(user: UserProfile | null): string;
function describe_optional_user_profile(user: UserProfile | null | undefined): string {
  return __ani_native_describe_optional_user_profile(user == undefined ? null : user);
}
native function __ani_native_maybe_user_profile(flag: boolean): UserProfile | null;
function maybe_user_profile(flag: boolean): UserProfile | null | undefined {
  let __ani_result = __ani_native_maybe_user_profile(flag);
  return __ani_result == null ? undefined : __ani_result;
}
native function __ani_native_maybe_user_profile_result(flag: boolean): UserProfile | null;
function maybe_user_profile_result(flag: boolean): UserProfile | null | undefined {
  let __ani_result = __ani_native_maybe_user_profile_result(flag);
  return __ani_result == null ? undefined : __ani_result;
}
native function choose_user_profile(flag: boolean): UserProfile | String;
native function describe_user_profile(user: UserProfile): string;
native function make_user_profile(id: int, name: string, active: boolean): UserProfile;
native function rename_user_profile(user: UserProfile, name: string): UserProfile;
native function user_profile_result(flag: boolean): UserProfile;



loadLibrary("ani_example_object_model");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-object-model");
let madeUser = make_user_profile(3, "ark", true);
__assert_eq_int("make_user_profile_id", madeUser.id, 3);
__assert_eq_string("make_user_profile_name", madeUser.name, "ark");
__assert_true("make_user_profile_active", madeUser.active);

let inputUser = new UserProfile();
inputUser.id = 8;
inputUser.name = "native";
inputUser.active = false;
__assert_eq_string(
  "describe_user_profile",
  describe_user_profile(inputUser),
  "8:native:inactive",
);

let renamedUser = rename_user_profile(inputUser, "renamed");
__assert_eq_string("rename_user_profile_name", renamedUser.name, "renamed");
__assert_eq_int("rename_user_profile_id", renamedUser.id, 8);

let chooseOk = choose_user_profile(true) as UserProfile;
__assert_eq_int("choose_user_profile_object_id", chooseOk.id, 7);
__assert_eq_string("choose_user_profile_object_name", chooseOk.name, "ani");
let chooseFallback = choose_user_profile(false) as String;
__assert_eq_string("choose_user_profile_string", chooseFallback.toString(), "no-user");

__assert_eq_string(
  "describe_optional_user_profile_undefined",
  describe_optional_user_profile(undefined),
  "none",
);
__assert_eq_string(
  "describe_optional_user_profile_null",
  describe_optional_user_profile(null),
  "none",
);
let maybeUser = maybe_user_profile(true);
__assert_true("maybe_user_profile_some", maybeUser != undefined);
let maybeUserObj = maybeUser as UserProfile;
__assert_eq_int("maybe_user_profile_some_id", maybeUserObj.id, 11);
__assert_eq_string("maybe_user_profile_some_name", maybeUserObj.name, "maybe");
__assert_true("maybe_user_profile_none", maybe_user_profile(false) == undefined);
__assert_eq_string(
  "describe_optional_user_profile_roundtrip",
  describe_optional_user_profile(maybeUser),
  "11:maybe:active",
);
let maybeResult = maybe_user_profile_result(true);
__assert_true("maybe_user_profile_result_some", maybeResult != undefined);
let maybeResultObj = maybeResult as UserProfile;
__assert_eq_int("maybe_user_profile_result_id", maybeResultObj.id, 12);
__assert_eq_string("maybe_user_profile_result_name", maybeResultObj.name, "result-option");
__assert_true("maybe_user_profile_result_none", maybe_user_profile_result(false) == undefined);

let resultOk = user_profile_result(true);
__assert_eq_int("user_profile_result_id", resultOk.id, 9);
__assert_eq_string("user_profile_result_name", resultOk.name, "result");
__assert_true("user_profile_result_inactive", !resultOk.active);
__assert_throws("user_profile_result_error", (): void => {
  user_profile_result(false);
});

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-object-model");
