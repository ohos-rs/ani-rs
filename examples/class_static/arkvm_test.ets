// Auto-generated arkvm smoke test for ani-example-class-static.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function call_static_int(cls: Class, method: Object): int;
export native function call_static_int_by_name(class_name: string, method_name: string, a: int, b: int): int;
export native function lookup_static_field(cls: Class): boolean;
export native function lookup_static_field_by_name(class_name: string, field_name: string): boolean;
export native function static_field_roundtrip_int(cls: Class, field: Object, value: int): int;
export native function static_field_roundtrip_int_by_name(class_name: string, field_name: string, value: int): int;



loadLibrary("ani_example_class_static");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-class-static");
class StaticHost {
  static COUNT: int = 0;
  static add(a: int, b: int): int {
    return a + b;
  }
}
__assert_bool(
  "lookup_static_field_by_name",
  lookup_static_field_by_name("arkvm_test.StaticHost", "COUNT"),
);
__assert_eq_int(
  "static_field_roundtrip_int_by_name",
  static_field_roundtrip_int_by_name("arkvm_test.StaticHost", "COUNT", 11),
  11,
);
__assert_eq_int(
  "call_static_int_by_name",
  call_static_int_by_name("arkvm_test.StaticHost", "add", 2, 3),
  5,
);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-class-static");
