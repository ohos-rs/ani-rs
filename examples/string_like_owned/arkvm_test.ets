// Auto-generated arkvm smoke test for ani-example-string-like-owned.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function append_borrowed_path_segment(path: string, tail: string): string;
export native function append_boxed_segment(path: string, tail: string): string;
export native function append_cow_segment(path: string, tail: string): string;
export native function append_os_segment(path: string, tail: string): string;
export native function borrowed_c_str_len(value: string): int;
export native function borrowed_c_str_literal(): string;
export native function borrowed_os_prefix(path: string, prefix: string): boolean;
export native function borrowed_os_str_literal(): string;
export native function borrowed_path_literal(): string;



loadLibrary("ani_example_string_like_owned");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-string-like-owned");
__assert_eq_string(
  "append_os_segment",
  append_os_segment("root", "tail"),
  "root/tail",
);
__assert_eq_string(
  "append_boxed_segment",
  append_boxed_segment("left", "right"),
  "left/right",
);
__assert_eq_string(
  "append_cow_segment",
  append_cow_segment("cow", "leaf"),
  "cow/leaf",
);
__assert_eq_string(
  "append_borrowed_path_segment",
  append_borrowed_path_segment("base", "tail"),
  "base/tail",
);
__assert_true(
  "borrowed_os_prefix",
  borrowed_os_prefix("prefix-value", "prefix"),
);
__assert_eq_int(
  "borrowed_c_str_len",
  borrowed_c_str_len("ffi"),
  3,
);
__assert_eq_string(
  "borrowed_path_literal",
  borrowed_path_literal(),
  "borrowed/path",
);
__assert_eq_string(
  "borrowed_os_str_literal",
  borrowed_os_str_literal(),
  "borrowed-os",
);
__assert_eq_string(
  "borrowed_c_str_literal",
  borrowed_c_str_literal(),
  "borrowed-c",
);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-string-like-owned");
