// Auto-generated arkvm smoke test for ani-example-constructor-overload.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

class Measure {
  private _kind: string = "";
  private _total: int = 0;
  native constructor(kind: string, total: int);
  native constructor(left: int, right: int);
  native __ani_native_get_kind(): string;
  get kind(): string {
    return this.__ani_native_get_kind();
  }
  native __ani_native_get_total(): int;
  get total(): int {
    return this.__ani_native_get_total();
  }
  native describe(): string;
}



loadLibrary("ani_example_constructor_overload");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-constructor-overload");
let pair = new Measure(2, 3);
__assert_eq_string("Measure.kind_pair", pair.kind, "pair");
__assert_eq_int("Measure.total_pair", pair.total, 5);
__assert_eq_string("Measure.describe_pair", pair.describe(), "pair:5");

let named = new Measure("named", 4);
__assert_eq_string("Measure.kind_named", named.kind, "named");
__assert_eq_int("Measure.total_named", named.total, 4);
__assert_eq_string("Measure.describe_named", named.describe(), "named:4");

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-constructor-overload");
