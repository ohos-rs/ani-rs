// Auto-generated arkvm smoke test for ani-example-template.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function array_get(arr: Object, index: int): boolean;
export native function array_length(arr: Object): int;
export native function container_get_int(ptr: long): int;
export native function container_get_string(ptr: long): string;
export native function container_set_int(ptr: long, value: int): void;
export native function container_set_string(ptr: long, value: string): void;
export native function create_int_container(value: int): long;
export native function create_pair(key: string, value: int): long;
export native function create_string_container(value: string): long;
export native function destroy_int_container(ptr: long): void;
export native function destroy_pair(ptr: long): void;
export native function destroy_string_container(ptr: long): void;
export native function get_first(first: Object, _second: Object): long;
export native function get_second(_first: Object, second: Object): long;
export native function identity(value: Object): long;
export native function pair_get_key(ptr: long): string;
export native function pair_get_value(ptr: long): int;



loadLibrary("ani_example_template");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-template");
let intContainer = create_int_container(10);
__assert_eq_int("container_get_int", container_get_int(intContainer), 10);
container_set_int(intContainer, 12);
__assert_eq_int("container_set_int", container_get_int(intContainer), 12);
destroy_int_container(intContainer);

let pair = create_pair("k", 9);
__assert_eq_string("pair_get_key", pair_get_key(pair), "k");
__assert_eq_int("pair_get_value", pair_get_value(pair), 9);
destroy_pair(pair);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-template");
