// Auto-generated arkvm smoke test for ani-example-wrap-ptr.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

native function add_ref(ptr: long): int;
native function create_db_connection(connection_string: string): long;
native function create_native_resource(id: int, name: string): long;
native function create_typed_ptr(type_id: int, value: long): long;
native function db_connect(ptr: long): boolean;
native function db_disconnect(ptr: long): void;
native function db_execute_query(ptr: long, query: string): int;
native function db_get_query_count(ptr: long): int;
native function db_is_connected(ptr: long): boolean;
native function destroy_db_connection(ptr: long): void;
native function destroy_native_resource(ptr: long): void;
native function get_ptr_type_id(encoded: long): int;
native function get_ptr_value(encoded: long): long;
native function get_ref_count(ptr: long): int;
native function get_resource_id(ptr: long): int;
native function get_resource_name(ptr: long): string;
native function release_ref(ptr: long): int;
native function resource_add_data(ptr: long, byte_: int): void;
native function resource_data_size(ptr: long): int;
native function set_resource_name(ptr: long, name: string): void;
native function validate_ptr_type(encoded: long, expected_type: int): boolean;



loadLibrary("ani_example_wrap_ptr");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-wrap-ptr");
let res = create_native_resource(7, "r1");
__assert_true("create_native_resource_handle", res > 0);
__assert_eq_int("get_resource_id", get_resource_id(res), 7);
__assert_eq_string("get_resource_name", get_resource_name(res), "r1");
set_resource_name(res, "r2");
__assert_eq_string("set_resource_name", get_resource_name(res), "r2");
destroy_native_resource(res);

let db = create_db_connection("db://local");
__assert_true("db_handle", db > 0);
__assert_true("db_connect", db_connect(db));
__assert_true("db_is_connected", db_is_connected(db));
__assert_eq_int("db_execute_query", db_execute_query(db, "select 1"), 1);
__assert_eq_int("db_get_query_count", db_get_query_count(db), 1);
db_disconnect(db);
destroy_db_connection(db);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-wrap-ptr");
