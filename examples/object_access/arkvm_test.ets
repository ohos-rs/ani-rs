// Auto-generated arkvm smoke test for ani-example-object-access.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function field_by_handle_int_roundtrip(obj: Object, value: int): int;
export native function field_by_name_int_roundtrip(obj: Object, value: int): int;
export native function field_ref_roundtrip(obj: Object, value: string): boolean;
export native function property_by_name_double_roundtrip(obj: Object, value: double): double;
export native function property_ref_roundtrip(obj: Object, value: string): boolean;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
promise.then(
(value: T): void => { __ani_rs_promise_resolve(token, value as Object); },
(reason: Object): void => { __ani_rs_promise_reject(token, reason); }
);
}



loadLibrary("ani_example_object_access");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-object-access");
class AccessTarget {
  counter: int;
  label: string;
  private _ratio: double;
  private _alias: string;

  constructor(counter: int, label: string, ratio: double, alias: string) {
    this.counter = counter;
    this.label = label;
    this._ratio = ratio;
    this._alias = alias;
  }

  get ratio(): double {
    return this._ratio;
  }

  set ratio(value: double) {
    this._ratio = value;
  }

  get alias(): string {
    return this._alias;
  }

  set alias(value: string) {
    this._alias = value;
  }
}

let target = new AccessTarget(1, "seed", 1.5, "alpha");
__assert_eq_int("field_by_name_int_roundtrip", field_by_name_int_roundtrip(target, 7), 7);
__assert_eq_int("field_by_name_int_state", target.counter, 7);
__assert_eq_int("field_by_handle_int_roundtrip", field_by_handle_int_roundtrip(target, 11), 11);
__assert_eq_int("field_by_handle_int_state", target.counter, 11);
__assert_true("field_ref_roundtrip", field_ref_roundtrip(target, "renamed"));
__assert_eq_string("field_ref_state", target.label, "renamed");
__assert_eq_double("property_by_name_double_roundtrip", property_by_name_double_roundtrip(target, 2.75), 2.75);
__assert_eq_double("property_by_name_double_state", target.ratio, 2.75);
__assert_true("property_ref_roundtrip", property_ref_roundtrip(target, "beta"));
__assert_eq_string("property_ref_state", target.alias, "beta");

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-object-access");
