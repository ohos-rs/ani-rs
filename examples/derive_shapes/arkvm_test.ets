// Auto-generated arkvm smoke test for ani-example-derive-shapes.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export class DeriveBox<T> {
  value: T = null as T;
}

export class DeriveMarker {
}

export class DerivePair<T, U> {
  field0: T = null as T;
  private __ani_property_field1: U = null as U;
  get field1(): U {
    return this.__ani_property_field1;
  }
  set field1(value: U) {
    this.__ani_property_field1 = value;
  }
}

export class ObjectBox<T> {
  value: T = null as T;
}

export class ObjectMarker {
}

export class ObjectPair<T, U> {
  field0: T = null as T;
  field1: U = null as U;
}

export native function describe_derive_pair(value: DerivePair<string, string>): string;
export native function describe_object_box(value: ObjectBox<string>): string;
export native function describe_object_pair(value: ObjectPair<string, string>): string;
export native function make_derive_box(value: string): DeriveBox<string>;
export native function make_derive_pair(left: string, right: string): DerivePair<string, string>;
export native function make_object_box(value: string): ObjectBox<string>;
export native function make_object_pair(left: string, right: string): ObjectPair<string, string>;
export native function marker_identity(value: DeriveMarker): boolean;
export native function next_user_id(id: long): long;
export native function object_marker_identity(value: ObjectMarker): boolean;
export native function read_derive_box(value: DeriveBox<string>): string;
export native function score_total(scores: ValueArray<int>): int;



loadLibrary("ani_example_derive_shapes");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-derive-shapes");
let deriveBox = make_derive_box("box");
__assert_eq_string("derive_box_value", deriveBox.value, "box");
__assert_eq_string("read_derive_box", read_derive_box(deriveBox), "box");

let derivePair = make_derive_pair("ani", "ark");
__assert_eq_string("derive_pair_field0", derivePair.field0, "ani");
__assert_eq_string("derive_pair_field1", derivePair.field1, "ark");
__assert_eq_string(
  "describe_derive_pair",
  describe_derive_pair(derivePair),
  "ani:ark",
);
__assert_true("marker_identity", marker_identity(new DeriveMarker()));

let objectBox = make_object_box("box");
__assert_eq_string("object_box_value", objectBox.value, "box");
__assert_eq_string(
  "describe_object_box",
  describe_object_box(objectBox),
  "box:box",
);

let objectPair = make_object_pair("left", "ark");
__assert_eq_string("object_pair_field0", objectPair.field0, "left");
__assert_eq_string("object_pair_field1", objectPair.field1, "ark");
__assert_eq_string(
  "describe_object_pair",
  describe_object_pair(objectPair),
  "left:ark",
);
__assert_true(
  "object_marker_identity",
  object_marker_identity(new ObjectMarker()),
);
__assert_eq_long("transparent_newtype", next_user_id(41), 42);
__assert_eq_int("array_newtype", score_total([2, 3, 4]), 9);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-derive-shapes");
