// Auto-generated arkvm smoke test for ani-example-array-generic.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export type AniRef = Object;

export class ArrayUser {
  id: int = 0;
  name: string = "";
}

export native function array_push_and_pop(value: AniRef): boolean;
export native function array_set_and_get(value: AniRef): boolean;
export native function join_string_array(values: Array<string>): string;
export native function join_string_vecdeque(values: Array<string>): string;
export native function make_i32_linked_list(): ValueArray<int>;
export native function make_string_array(): Array<string>;
export native function make_string_vecdeque(): Array<string>;
export native function make_user_array(): Array<ArrayUser>;
export native function sum_i32_linked_list(values: ValueArray<int>): int;
export native function summarize_user_array(values: Array<ArrayUser>): string;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
promise.then(
(value: T): void => { __ani_rs_promise_resolve(token, value as Object); },
(reason: Object): void => { __ani_rs_promise_reject(token, reason); }
);
}



loadLibrary("ani_example_array_generic");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-array-generic");
class ArrayPayload {
  value: int;
  constructor(value: int) {
    this.value = value;
  }
}
let pushObj: Object = new ArrayPayload(1);
let setObj: Object = new ArrayPayload(2);
__assert_bool("array_push_and_pop_bool", array_push_and_pop(pushObj));
__assert_bool("array_set_and_get_bool", array_set_and_get(setObj));
__assert_eq_string(
  "join_string_array",
  join_string_array(["ani", "ark", "vm"]),
  "ani|ark|vm",
);
let producedStrings = make_string_array();
__assert_eq_int("make_string_array_len", producedStrings.length, 3);
__assert_eq_string("make_string_array_last", producedStrings[2], "vm");
__assert_eq_string(
  "join_string_vecdeque",
  join_string_vecdeque(["front", "middle", "back"]),
  "front|middle|back",
);
let producedDeque = make_string_vecdeque();
__assert_eq_int("make_string_vecdeque_len", producedDeque.length, 3);
__assert_eq_string("make_string_vecdeque_first", producedDeque[0], "front");
__assert_eq_int(
  "sum_i32_linked_list",
  sum_i32_linked_list([2, 3, 4]),
  9,
);
let producedLinkedList = make_i32_linked_list();
__assert_eq_int("make_i32_linked_list_len", producedLinkedList.length, 3);
__assert_eq_int("make_i32_linked_list_tail", producedLinkedList[2], 5);
let userA = new ArrayUser();
userA.id = 1;
userA.name = "alice";
let userB = new ArrayUser();
userB.id = 2;
userB.name = "bob";
__assert_eq_string(
  "summarize_user_array",
  summarize_user_array([userA, userB]),
  "1#alice|2#bob",
);
let producedUsers = make_user_array();
__assert_eq_int("make_user_array_len", producedUsers.length, 2);
let firstUser = producedUsers[0] as ArrayUser;
__assert_eq_int("make_user_array_first_id", firstUser.id, 7);
__assert_eq_string("make_user_array_first_name", firstUser.name, "alice");

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-array-generic");
