// Auto-generated arkvm smoke test for ani-example-object-runtime.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function clear_label_by_name(obj: Object): string;
export native function compare_by_name(obj: Object, left: int, right: int): string;
export native function create_runtime_box(value: int, label: string): string;
export native function describe_by_handle(obj: Object): string;
export native function describe_by_name_zero(obj: Object): string;
export native function is_positive_by_name(obj: Object): boolean;
export native function is_runtime_box_instance(obj: Object): boolean;
export native function runtime_box_assignable_to_base(obj: Object): boolean;
export native function runtime_box_has_super(obj: Object): boolean;
export native function sum_by_name(obj: Object, left: int, right: int): int;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
promise.then(
(value: T): void => { __ani_rs_promise_resolve(token, value as Object); },
(reason: Object): void => { __ani_rs_promise_reject(token, reason); }
);
}



loadLibrary("ani_example_object_runtime");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-object-runtime");
class RuntimeBox {
  value: int;
  label: string;

  constructor(value: int, label: string) {
    this.value = value;
    this.label = label;
  }

  sumNumbers(left: int, right: int): int {
    return this.value + left + right;
  }

  compareNumbers(left: int, right: int): string {
    return left == right ? "eq" : "ne";
  }

  describe(): string {
    return this.label + ":" + this.value;
  }

  isPositive(): boolean {
    return this.value > 0;
  }

  clearLabel(): void {
    this.label = "cleared";
  }
}

class RuntimeSubBox extends RuntimeBox {
  constructor(value: int, label: string) {
    super(value, label);
  }
}

__assert_eq_string("create_runtime_box", create_runtime_box(5, "seed"), "seed:5");
let created = new RuntimeBox(5, "seed");
__assert_eq_int("sum_by_name", sum_by_name(created, 2, 3), 10);
__assert_eq_string("compare_by_name_eq", compare_by_name(created, 4, 4), "eq");
__assert_eq_string("compare_by_name_ne", compare_by_name(created, 4, 5), "ne");
__assert_eq_string("describe_by_name_zero", describe_by_name_zero(created), "seed:5");
__assert_true("is_positive_by_name", is_positive_by_name(created));
__assert_eq_string("clear_label_by_name", clear_label_by_name(created), "cleared");
__assert_eq_string("describe_by_handle", describe_by_handle(created), "cleared:5");
__assert_true("is_runtime_box_instance_base", is_runtime_box_instance(created));
let derived = new RuntimeSubBox(9, "child");
__assert_true("is_runtime_box_instance_sub", is_runtime_box_instance(derived));
__assert_true("runtime_box_assignable_to_base", runtime_box_assignable_to_base(derived));
__assert_true("runtime_box_has_super", runtime_box_has_super(derived));

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-object-runtime");
