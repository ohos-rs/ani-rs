// Auto-generated arkvm smoke test for ani-example-any-dynamic.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

native function dynamic_call(func: Object, arg: Object): boolean;
native function dynamic_construct(ctor: Object, arg0: Object): boolean;
native function dynamic_get_set(obj: Object, value: Object): boolean;


loadLibrary("ani_example_any_dynamic");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-any-dynamic");
class DynamicAnyObject {
  value: Object;
  constructor(v: Object) {
    this.value = v;
  }
}
let anyObj: Object = new DynamicAnyObject(new DynamicAnyObject(new Object()));
let anyVal: Object = new DynamicAnyObject(new Object());
__assert_bool("dynamic_get_set_bool", dynamic_get_set(anyObj, anyVal));
function identity(x: Object): Object {
  return x;
}
__assert_bool("dynamic_call_bool", dynamic_call(identity, anyVal));
function DynamicCtor(v: Object): Object {
  return v;
}
let dynamicConstructOk = true;
try {
  dynamicConstructOk = dynamic_construct(DynamicCtor, anyVal);
} catch (_e) {
  dynamicConstructOk = false;
}
__assert_bool("dynamic_construct_bool", dynamicConstructOk);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-any-dynamic");
