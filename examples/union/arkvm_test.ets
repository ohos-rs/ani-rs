// Auto-generated arkvm smoke test for ani-example-union.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function create_by_type(type_code: int, int_val: int, str_val: string): string;
export native function get_type_code(value_type: int): int;
export native function handle_four_types(value: string | int | boolean | double): string;
export native function handle_string_or_int_either(value: string | int): string;
export native function handle_three_types(value: string | int | boolean): string;
export native function return_either(use_string: boolean): string | int;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
promise.then(
(value: T): void => { __ani_rs_promise_resolve(token, value as Object); },
(reason: Object): void => { __ani_rs_promise_reject(token, reason); }
);
}
export native function __ani_rs_cancel_runtime_task(token: long, reason: Object): void;
export class AniCancelHandle {
private token: long;
constructor(token: long) { this.token = token; }
cancel(reason: Object): void { __ani_rs_cancel_runtime_task(this.token, reason); }
}



loadLibrary("ani_example_union");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-union");
__assert_eq_string(
  "handle_string_or_int_either_string",
  handle_string_or_int_either(new String("ani")),
  "String: ani",
);
__assert_eq_string(
  "handle_string_or_int_either_int",
  handle_string_or_int_either(7),
  "Int: 7",
);
__assert_eq_string(
  "handle_three_types_bool",
  handle_three_types(true),
  "Boolean: true",
);
__assert_eq_string(
  "handle_four_types_double",
  handle_four_types(3.5),
  "Double: 3.5",
);
let eitherString = return_either(true) as String;
__assert_eq_string("return_either_string", eitherString.toString(), "Hello from Either!");
let eitherInt = return_either(false) as Int;
__assert_eq_string("return_either_int", eitherInt.toString(), "42");
__assert_eq_string("create_by_type_int", create_by_type(1, 9, "x"), "Int: 9");
__assert_eq_int("get_type_code", get_type_code(2), 2);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-union");
