// Auto-generated arkvm smoke test for ani-example-ets-declaration.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export namespace AniMath {
  export namespace Utils {
    export native function sqrt(x: double): double;
    export native function sum3(a: int, b: int, c: int): int;
  }
}

export namespace example {
  export class Person {
    private _name: string = "";
    private _score: int = 0;
    native constructor(name: string, score: int);
    native __ani_native_get_name(): string;
    get name(): string {
      return this.__ani_native_get_name();
    }
    native __ani_native_get_score(): int;
    get score(): int {
      return this.__ani_native_get_score();
    }
    native __ani_native_set_score(score: int): void;
    set score(score: int) {
      this.__ani_native_set_score(score);
    }
    static native species(): string;
    native label(): string;
  }
}

export native function add(a: int, b: int): int;



loadLibrary("ani_example_ets_declaration");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-ets-declaration");
__assert_eq_int("add", add(3, 4), 7);
__assert_eq_double("AniMath.Utils.sqrt", AniMath.Utils.sqrt(9.0), 3.0);
__assert_eq_int("AniMath.Utils.sum3", AniMath.Utils.sum3(1, 2, 3), 6);
let person = new example.Person("ani-rs", 7);
__assert_eq_string("example.Person.name", person.name, "ani-rs");
__assert_eq_int("example.Person.score", person.score, 7);
person.score = 9;
__assert_eq_int("example.Person.score_after_set", person.score, 9);
__assert_eq_string("example.Person.label", person.label(), "ani-rs#9");
__assert_eq_string("example.Person.species", example.Person.species(), "human");

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-ets-declaration");
