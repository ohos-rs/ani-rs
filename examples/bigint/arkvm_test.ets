// Auto-generated arkvm smoke test for ani-example-bigint.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function big_int_abs(value: long): long;
export native function big_int_add(a: long, b: long): long;
export native function big_int_and(a: long, b: long): long;
export native function big_int_bit_length(value: long): int;
export native function big_int_compare(a: long, b: long): int;
export native function big_int_divide(a: long, b: long): long;
export native function big_int_is_negative(value: long): boolean;
export native function big_int_is_zero(value: long): boolean;
export native function big_int_modulo(a: long, b: long): long;
export native function big_int_multiply(a: long, b: long): long;
export native function big_int_negate(value: long): long;
export native function big_int_or(a: long, b: long): long;
export native function big_int_pow(base: long, exp: int): long;
export native function big_int_shl(a: long, bits: int): long;
export native function big_int_shr(a: long, bits: int): long;
export native function big_int_subtract(a: long, b: long): long;
export native function big_int_to_long(value: long): long;
export native function big_int_xor(a: long, b: long): long;
export native function create_big_int(value: long): long;



loadLibrary("ani_example_bigint");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-bigint");
__assert_eq_long("big_int_add", big_int_add(7, 5), 12);
__assert_eq_long("big_int_subtract", big_int_subtract(9, 4), 5);
__assert_eq_long("big_int_multiply", big_int_multiply(3, 4), 12);
__assert_true("big_int_compare_lt", big_int_compare(2, 5) < 0);
__assert_true("big_int_is_zero", big_int_is_zero(0));

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-bigint");
