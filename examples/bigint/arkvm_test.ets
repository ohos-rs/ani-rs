// Auto-generated arkvm smoke test for ani-example-bigint.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function big_int_decimal_digits(value: bigint): int;
export native function big_int_from_decimal(value: string): bigint;
export native function big_int_identity(value: bigint): bigint;
export native function big_int_is_negative(value: bigint): boolean;
export native function big_int_to_decimal(value: bigint): string;
export native function big_int_to_i64_lossless(value: bigint): long;



loadLibrary("ani_example_bigint");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-bigint");
let huge: bigint = new BigInt("1234567890123456789012345678901234567890");
__assert_eq_string("big_int_to_decimal", big_int_to_decimal(huge), "1234567890123456789012345678901234567890");
__assert_eq_string("big_int_identity", big_int_identity(huge).toString(), huge.toString());
__assert_eq_string("big_int_from_decimal", big_int_from_decimal("-999999999999999999999999").toString(), "-999999999999999999999999");
__assert_eq_int("big_int_decimal_digits", big_int_decimal_digits(huge), 40);
__assert_true("big_int_is_negative", big_int_is_negative(new BigInt("-1")));
__assert_eq_long("big_int_to_i64_lossless", big_int_to_i64_lossless(new BigInt("9223372036854775807")), 9223372036854775807);
__assert_throws("big_int_to_i64_overflow", (): void => {
  big_int_to_i64_lossless(huge);
});

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-bigint");
