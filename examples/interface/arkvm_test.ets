// Auto-generated arkvm smoke test for ani-example-interface.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

native function compare_values(a: long, b: long): int;
native function create_comparable(value: int): long;
native function create_int_iterator(start: int, end: int): long;
native function create_serializable(data: string): long;
native function deserialize(ptr: long, json: string): void;
native function destroy_comparable(ptr: long): void;
native function destroy_iterator(ptr: long): void;
native function destroy_serializable(ptr: long): void;
native function get_data(ptr: long): string;
native function iterator_has_next(ptr: long): boolean;
native function iterator_next(ptr: long): int;
native function iterator_reset(ptr: long, start: int): void;
native function serialize(ptr: long): string;


loadLibrary("ani_example_interface");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-interface");
let c1 = create_comparable(10);
let c2 = create_comparable(20);
__assert_true("create_comparable_handles", c1 > 0 && c2 > 0);
__assert_true("compare_values_lt", compare_values(c1, c2) < 0);
destroy_comparable(c1);
destroy_comparable(c2);

let s = create_serializable("hello");
__assert_true("create_serializable_handle", s > 0);
__assert_true("serialize_contains_data", serialize(s).length > 0);
deserialize(s, "{\"data\":\"world\"}");
__assert_eq_string("get_data", get_data(s), "world");
destroy_serializable(s);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-interface");
