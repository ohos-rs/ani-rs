// Auto-generated arkvm smoke test for ani-example-record.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function create_record(): long;
export native function create_record_direct(): Record<string, int>;
export native function create_string_record(): long;
export native function destroy_record(record_: long): void;
export native function destroy_string_record(record_: long): void;
export native function record_clear(record_: long): void;
export native function record_delete(record_: long, key: string): boolean;
export native function record_from_json(json: string): long;
export native function record_get(record_: long, key: string): int;
export native function record_has(record_: long, key: string): boolean;
export native function record_set(record_: long, key: string, value: int): void;
export native function record_size(record_: long): int;
export native function record_sum(entry: Record<string, int>): int;
export native function record_to_json(record_: long): string;
export native function string_record_get(record_: long, key: string): string;
export native function string_record_set(record_: long, key: string, value: string): void;
export native function string_record_to_json(record_: long): string;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
promise.then(
(value: T): void => { __ani_rs_promise_resolve(token, value as Object); },
(reason: Object): void => { __ani_rs_promise_reject(token, reason); }
);
}
export native function __ani_rs_cancel_runtime_task(token: long, reason: Object): void;
export class AniCancelHandle {
private token: long;
constructor(token: long) { this.token = token; }
cancel(reason: Object): void { __ani_rs_cancel_runtime_task(this.token, reason); }
}



loadLibrary("ani_example_record");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-record");
let recordPtr = create_record();
record_set(recordPtr, "a", 1);
record_set(recordPtr, "b", 2);
__assert_eq_int("record_size", record_size(recordPtr), 2);
__assert_eq_int("record_get", record_get(recordPtr, "a"), 1);
__assert_true("record_has", record_has(recordPtr, "b"));
destroy_record(recordPtr);

let directRecord = create_record_direct();
__assert_eq_int("record_sum_direct", record_sum(directRecord), 44);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-record");
