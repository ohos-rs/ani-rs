// Auto-generated arkvm smoke test for ani-example-function.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function call_bool_callback(callback: (arg0: int) => boolean, value: int): boolean;
export native function call_double_callback(callback: (arg0: double) => double, value: double): double;
export native function call_if(callback: (arg0: int) => int, value: int, condition: boolean): int;
export native function call_mixed_args(callback: (arg0: string, arg1: int, arg2: double) => string, name: string, count: int, weight: double): string;
export native function call_no_args_callback(callback: () => string): string;
export native function call_string_callback(callback: (arg0: string) => string, input: string): string;
export native function call_void_callback(callback: (arg0: string) => void, message: string): void;
export native function call_with_callback(callback: (arg0: int) => int, value: int): int;
export native function call_with_four_args(callback: (arg0: int, arg1: int, arg2: int, arg3: int) => int, a: int, b: int, c: int, d: int): int;
export native function call_with_multi_args(callback: (arg0: int, arg1: int) => int, a: int, b: int): int;
export native function call_with_three_args(callback: (arg0: int, arg1: int, arg2: int) => int, a: int, b: int, c: int): int;
export native function chain_callbacks(first: (arg0: int) => int, second: (arg0: int) => int, value: int): int;
export native function clear_callback(): void;
export native function echo_function_ref(callback: (arg0: string) => string): (arg0: string) => string;
export native function echo_scoped_string_callback(callback: (arg0: string) => string): (arg0: string) => string;
export native function has_callback(): boolean;
export native function has_string_transformer(): boolean;
export native function invoke_multi_arg_callback(a: int, b: int, c: int): int;
export native function invoke_stored_callback(value: int): int;
export native function reduce_values(callback: (arg0: int, arg1: int) => int, initial: int, v1: int, v2: int, v3: int): int;
export native function register_callback(callback: (arg0: int) => int): void;
export native function register_multi_arg_callback(callback: (arg0: int, arg1: int, arg2: int) => int): void;
export native function register_string_transformer(callback: (arg0: string) => string): void;
export native function repeat_callback(callback: (arg0: int) => int, value: int, times: int): int;
export native function transform_string(input: string): string;



loadLibrary("ani_example_function");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-function");
function noArgsOk(): string {
  return "ok";
}
function wrapValue(input: string): string {
  return "wrapped:" + input;
}
function recursiveString(target: string, current: string): string {
  if (target == current) {
    return current;
  }
  return recursiveString(target, current + "1");
}
let closureValue = "closure:";
let closureFn = (input: string): string => {
  return closureValue + input;
};
let nestedFn = (input: string): string => {
  let prefix = "hello ";
  let inner = (value: string): string => {
    return prefix + value;
  };
  return inner(input);
};
let recursiveFn = (input: string): string => {
  return recursiveString("hello1111111111", input);
};
clear_callback();
__assert_true("has_callback_false", !has_callback());
register_string_transformer(wrapValue);
__assert_true("has_string_transformer_true", has_string_transformer());
__assert_eq_string("transform_string_basic", transform_string("ani"), "wrapped:ani");
__assert_eq_string("call_no_args_callback", call_no_args_callback(noArgsOk), "ok");
__assert_eq_string("call_string_callback_basic", call_string_callback(wrapValue, "ark"), "wrapped:ark");
__assert_eq_string("call_string_callback_closure", call_string_callback(closureFn, "arkts"), "closure:arkts");
__assert_eq_string("call_string_callback_nested", call_string_callback(nestedFn, "world"), "hello world");
__assert_eq_string("call_string_callback_recursive", call_string_callback(recursiveFn, "hello"), "hello1111111111");
register_string_transformer(closureFn);
__assert_eq_string("transform_string_closure", transform_string("vm"), "closure:vm");
register_string_transformer(nestedFn);
__assert_eq_string("transform_string_nested", transform_string("arkvm"), "hello arkvm");
let echoedScoped = echo_scoped_string_callback(wrapValue);
__assert_eq_string("echo_scoped_string_callback", echoedScoped("roundtrip"), "wrapped:roundtrip");
let echoedGlobal = echo_function_ref(closureFn);
__assert_eq_string("echo_function_ref", echoedGlobal("again"), "closure:again");

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-function");
