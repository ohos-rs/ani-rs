// Auto-generated arkvm smoke test for ani-example-reference.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function clear_all_named_objects(): void;
export native function clear_compare_object(): void;
export native function clear_stored_object(): void;
export native function clone_stored_object(): boolean;
export native function compare_stored_references(): boolean;
export native function get_named_object_count(): int;
export native function has_named_object(name: string): boolean;
export native function has_stored_object(): boolean;
export native function remove_named_object(name: string): boolean;
export native function set_compare_object(obj: Object): void;
export native function store_named_object(name: string, obj: Object): void;
export native function store_object(obj: Object): void;
export native function use_stored_object(): boolean;



loadLibrary("ani_example_reference");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-reference");
clear_stored_object();
__assert_true("has_stored_object_false", !has_stored_object());
class RefObject {
  name: string;
  value: int;
  constructor(name: string, value: int) {
    this.name = name;
    this.value = value;
  }
}
let refObj = new RefObject("ani", 1);
store_object(refObj);
__assert_true("has_stored_object_true", has_stored_object());
__assert_true("use_stored_object", use_stored_object());
set_compare_object(refObj);
__assert_bool("compare_stored_references_bool", compare_stored_references());
__assert_bool("clone_stored_object_bool", clone_stored_object());
clear_stored_object();
clear_compare_object();

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-reference");
