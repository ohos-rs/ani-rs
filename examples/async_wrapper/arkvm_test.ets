// Auto-generated arkvm smoke test for ani-example-async-wrapper.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export namespace example {
  export class MyClass {
    static native create(_name: string): long;
    native get_info(): string;
  }
}

export native function async_check_status(task_id: int): boolean;
export native function async_compute_start(input: int): int;
export native function async_fetch(url: string): string;
export native function async_get_result(task_id: int): int;
export native function async_square(n: int): int;
export native function batch_compute(count: int): long;
export native function promise_delayed(delay_ms: int, value: string): long;
export native function promise_maybe_succeed(should_succeed: boolean, value: int): long;
export native function promise_reject(message: string): long;
export native function promise_resolve_int(value: int): long;
export native function promise_resolve_string(value: string): long;
export native function tokio_delayed_square(input: int, delay_ms: int): Promise<int>;
export native function tokio_delayed_square_async(input: int, delay_ms: int): Promise<int>;
export native function tokio_fail(message: string): Promise<string>;
export native function tokio_fail_async(message: string): Promise<string>;
export native function tokio_fetch_text(url: string): Promise<string>;
export native function tokio_fetch_text_async(url: string): Promise<string>;
export native function tokio_void_async(delay_ms: int): Promise<void>;



loadLibrary("ani_example_async_wrapper");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-async-wrapper");
function main(): void {
  console.log("[arkvm] smoke start: ani-example-async-wrapper");
  __assert_eq_int("async_square", async_square(6), 36);
  let taskId = async_compute_start(5);
  __assert_true("async_compute_start_non_negative", taskId >= 0);
  __assert_bool("async_check_status_type", async_check_status(taskId));
  __assert_true("batch_compute_non_negative", batch_compute(3) >= 0);
  let promisedSquare: int = waitForCompletion(() => tokio_delayed_square(7, 10));
  __assert_eq_int("tokio_delayed_square", promisedSquare, 49);
  let promisedSquareAsync: int = waitForCompletion(() => tokio_delayed_square_async(7, 10));
  __assert_eq_int("tokio_delayed_square_async", promisedSquareAsync, 49);
  let promisedText: string = waitForCompletion(() => tokio_fetch_text("ani://tokio"));
  __assert_eq_string("tokio_fetch_text", promisedText, "Response from: ani://tokio");
  let promisedTextAsync: string = waitForCompletion(() => tokio_fetch_text_async("ani://tokio"));
  __assert_eq_string("tokio_fetch_text_async", promisedTextAsync, "Response from: ani://tokio");
  let tokioRejected: boolean = waitForCompletion(async (): Promise<boolean> => {
    try {
      await tokio_fail("boom");
      return false;
    } catch (_e) {
      return true;
    }
  });
  __assert_true("tokio_fail", tokioRejected);
  let tokioRejectedAsync: boolean = waitForCompletion(async (): Promise<boolean> => {
    try {
      await tokio_fail_async("boom2");
      return false;
    } catch (_e) {
      return true;
    }
  });
  __assert_true("tokio_fail_async", tokioRejectedAsync);
  let tokioVoidAsync: boolean = waitForCompletion(async (): Promise<boolean> => {
    await tokio_void_async(1);
    return true;
  });
  __assert_true("tokio_void_async", tokioVoidAsync);

  if (__ani_fail_count > 0) {
    throw new Error("arkvm assertions failed: " + __ani_fail_count);
  }
  console.log("[arkvm] smoke done: ani-example-async-wrapper");
}

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-async-wrapper");
