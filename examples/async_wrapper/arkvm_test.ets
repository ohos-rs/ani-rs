// Auto-generated arkvm smoke test for ani-example-async-wrapper.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export type AniRef = Object;

export class AsyncAccessorBox {
  native constructor(initial_note: string);
  native __ani_native_async_accessor_box_note(): string;
  get note(): string {
    return this.__ani_native_async_accessor_box_note();
  }
  native __ani_native_async_accessor_box_set_note(note: string): void;
  set note(note: string) {
    this.__ani_native_async_accessor_box_set_note(note);
  }
  native __ani_native_async_accessor_box_summary(): string;
  get summary(): string {
    return this.__ani_native_async_accessor_box_summary();
  }
}

export class AsyncCtorBox {
  native constructor(label: string, total: int);
  native __ani_native_async_ctor_box_note(): string;
  get note(): string {
    return this.__ani_native_async_ctor_box_note();
  }
  native __ani_native_async_ctor_box_total(): int;
  get total(): int {
    return this.__ani_native_async_ctor_box_total();
  }
}

export class AsyncWidget {
  private _tag: int = 0;
  native constructor();
  static native class_handle_ready(): Promise<boolean>;
  native bump(delta: int): Promise<int>;
  native describe(): Promise<string>;
  native this_handle_ready(): Promise<boolean>;
}

export namespace example {
  export class MyClass {
    static native create(_name: string): long;
    native get_info(): string;
  }
}

export native function async_call_scoped_callback(callback: (arg0: string) => string, value: string): Promise<string>;
export native function async_check_status(task_id: int): boolean;
export native function async_compute_start(input: int): int;
export native function async_fetch(url: string): string;
export native function async_get_result(task_id: int): int;
export native function async_object_strict_equals(lhs: Object, rhs: Object): Promise<boolean>;
export native function async_ref_roundtrip(value: AniRef): Promise<boolean>;
export native function async_square(n: int): int;
export native function batch_compute(count: int): long;
export native function env_roundtrip(value: string): Promise<string>;
export native function promise_delayed(delay_ms: int, value: string): long;
export native function promise_maybe_succeed(should_succeed: boolean, value: int): long;
export native function promise_reject(message: string): long;
export native function promise_resolve_int(value: int): long;
export native function promise_resolve_string(value: string): long;
export native function signature_override_echo(value: string): Promise<string>;
export native function tokio_delayed_square(input: int, delay_ms: int): Promise<int>;
export native function tokio_delayed_square_async(input: int, delay_ms: int): Promise<int>;
export native function tokio_fail(message: string): Promise<string>;
export native function tokio_fail_async(message: string): Promise<string>;
export native function tokio_fetch_text(url: string): Promise<string>;
export native function tokio_fetch_text_async(url: string): Promise<string>;
export native function tokio_manual_ref_container_ready(value: Object): Promise<boolean>;
export native function tokio_void_async(delay_ms: int): Promise<void>;



loadLibrary("ani_example_async_wrapper");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-async-wrapper");
function main(): void {
  console.log("[arkvm] smoke start: ani-example-async-wrapper");
  __assert_eq_int("async_square", async_square(6), 36);
  let taskId = async_compute_start(5);
  __assert_true("async_compute_start_non_negative", taskId >= 0);
  __assert_bool("async_check_status_type", async_check_status(taskId));
  __assert_true("batch_compute_non_negative", batch_compute(3) >= 0);
  let promisedSquare: int = waitForCompletion(() => tokio_delayed_square(7, 10));
  __assert_eq_int("tokio_delayed_square", promisedSquare, 49);
  let promisedSquareAsync: int = waitForCompletion(() => tokio_delayed_square_async(7, 10));
  __assert_eq_int("tokio_delayed_square_async", promisedSquareAsync, 49);
  let promisedText: string = waitForCompletion(() => tokio_fetch_text("ani://tokio"));
  __assert_eq_string("tokio_fetch_text", promisedText, "Response from: ani://tokio");
  let promisedTextAsync: string = waitForCompletion(() => tokio_fetch_text_async("ani://tokio"));
  __assert_eq_string("tokio_fetch_text_async", promisedTextAsync, "Response from: ani://tokio");
  let tokioRejected: boolean = waitForCompletion(async (): Promise<boolean> => {
    try {
      await tokio_fail("boom");
      return false;
    } catch (_e) {
      return true;
    }
  });
  __assert_true("tokio_fail", tokioRejected);
  let tokioRejectedAsync: boolean = waitForCompletion(async (): Promise<boolean> => {
    try {
      await tokio_fail_async("boom2");
      return false;
    } catch (_e) {
      return true;
    }
  });
  __assert_true("tokio_fail_async", tokioRejectedAsync);
  let tokioVoidAsync: boolean = waitForCompletion(async (): Promise<boolean> => {
    await tokio_void_async(1);
    return true;
  });
  __assert_true("tokio_void_async", tokioVoidAsync);
  let envText: string = waitForCompletion(() => env_roundtrip("hello"));
  __assert_eq_string("env_roundtrip", envText, "env:hello");
  let refPayload: Object = new Object();
  let sameObject: boolean = waitForCompletion(() => async_object_strict_equals(refPayload, refPayload));
  __assert_true("async_object_strict_equals_same", sameObject);
  let otherObject: boolean = waitForCompletion(() => async_object_strict_equals(refPayload, new Object()));
  __assert_true("async_object_strict_equals_diff", !otherObject);
  let refRoundtrip: boolean = waitForCompletion(() => async_ref_roundtrip(refPayload));
  __assert_true("async_ref_roundtrip", refRoundtrip);
  let asyncStringCallback = (input: string): string => {
    return "cb:" + input;
  };
  let scopedCallbackText: string = waitForCompletion(() => async_call_scoped_callback(asyncStringCallback, "ark"));
  __assert_eq_string("async_call_scoped_callback", scopedCallbackText, "cb:ark");
  let manualContainerReady: boolean = waitForCompletion(() => tokio_manual_ref_container_ready(refPayload));
  __assert_true("tokio_manual_ref_container_ready", manualContainerReady);
  let signatureText: string = waitForCompletion(() => signature_override_echo("value"));
  __assert_eq_string("signature_override_echo", signatureText, "sig:value");

  let ctorBox = new AsyncCtorBox("demo", 4);
  __assert_eq_int("async_constructor_total", ctorBox.total, 4);
  __assert_eq_string("async_constructor_note", ctorBox.note, "ctor:demo");

  let accessorBox = new AsyncAccessorBox("start");
  __assert_eq_string("accessor_constructor_note", accessorBox.note, "start");
  __assert_eq_string("async_getter_summary", accessorBox.summary, "note:start");
  accessorBox.note = "changed";
  __assert_eq_string("async_setter_note", accessorBox.note, "changed");

  let classReady: boolean = waitForCompletion(() => AsyncWidget.class_handle_ready());
  __assert_true("async_class_injection", classReady);

  let widget = new AsyncWidget();
  let firstBump: int = waitForCompletion(() => widget.bump(2));
  __assert_eq_int("async_receiver_bump_first", firstBump, 2);
  let secondBump: int = waitForCompletion(() => widget.bump(1));
  __assert_eq_int("async_receiver_bump_second", secondBump, 3);
  let widgetDesc: string = waitForCompletion(() => widget.describe());
  __assert_eq_string("async_receiver_describe", widgetDesc, "widget:3");
  let thisReady: boolean = waitForCompletion(() => widget.this_handle_ready());
  __assert_true("async_this_injection", thisReady);

  if (__ani_fail_count > 0) {
    throw new Error("arkvm assertions failed: " + __ani_fail_count);
  }
  console.log("[arkvm] smoke done: ani-example-async-wrapper");
}

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-async-wrapper");
