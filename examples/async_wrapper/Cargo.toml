[package]
name = "ani-example-async-wrapper"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ani = { workspace = true, features = ["tokio_rt"] }
ani-derive = { workspace = true }
tokio = { version = "1", features = ["time"] }
