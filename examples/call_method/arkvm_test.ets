// Auto-generated arkvm smoke test for ani-example-call-method.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

native function call_add(_calc_ptr: long, a: int, b: int): int;
native function call_get_number(obj_ptr: long): double;
native function call_get_string(obj_ptr: long): string;
native function call_greet(greeter_ptr: long, name: string): string;
native function call_static_square(n: int): int;
native function call_with_multiple_args(obj_ptr: long, int_val: int, double_val: double, str_val: string): string;
native function get_property_int(target: Object): int;
native function get_singleton(): long;
native function invoke_object_method_int(target: Object, method_name: string): int;


loadLibrary("ani_example_call_method");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-call-method");
__assert_eq_int("call_static_square", call_static_square(7), 49);
class MethodBox {
  value: int;
  constructor(v: int) {
    this.value = v;
  }
  getVal(): int {
    return this.value;
  }
}
let methodVal = invoke_object_method_int(new MethodBox(9), "getVal");
__assert_true("invoke_object_method_int_non_negative", methodVal >= 0);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-call-method");
