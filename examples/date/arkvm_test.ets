// Auto-generated arkvm smoke test for ani-example-date.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export native function date_add_days(value: Date, days: int): Date;
export native function date_from_millis(millis: double): Date;
export native function date_identity(value: Date): Date;
export native function date_is_before(first: Date, second: Date): boolean;
export native function date_to_millis(value: Date): double;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
  promise.then(
    (value: T): void => {
      __ani_rs_promise_resolve(token, value as Object);
    },
    (reason: Object): void => {
      __ani_rs_promise_reject(token, reason);
    },
  );
}
export native function __ani_rs_cancel_runtime_task(token: long, reason: Object): void;
export class AniCancelHandle {
  private token: long;
  constructor(token: long) {
    this.token = token;
  }
  cancel(reason: Object): void {
    __ani_rs_cancel_runtime_task(this.token, reason);
  }
}



loadLibrary("ani_example_date");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-date");
let epochMillis: long = 1700000000123;
let sample = new Date(epochMillis);
__assert_eq_double("date_to_millis", date_to_millis(sample), 1700000000123.0);
__assert_eq_long("date_identity", date_identity(sample).getTime(), epochMillis);
__assert_eq_long("date_from_millis", date_from_millis(1700000000123.0).getTime(), epochMillis);
__assert_eq_long("date_add_days", date_add_days(sample, 2).getTime(), epochMillis + 172800000);
__assert_eq_long("date_add_days_negative", date_add_days(sample, -1).getTime(), epochMillis - 86400000);
__assert_true("date_is_before", date_is_before(sample, new Date(epochMillis + 1)));
__assert_true("date_is_not_before", !date_is_before(sample, sample));
__assert_eq_double("date_pre_epoch", date_to_millis(new Date(-86400000)), -86400000.0);
__assert_throws("date_from_millis_nan", (): void => {
  date_from_millis(Double.NaN);
});

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-date");
