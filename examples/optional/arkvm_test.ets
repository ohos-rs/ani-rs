// Auto-generated arkvm smoke test for ani-example-optional.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

native function count_provided_args(): int;
native function with_default_simple(value: int, multiplier: int): int;
native function with_multiple_optional(a: int, b: Int | null, c: Int | null, d: Int | null): int;
native function with_optional_boolean(value: int, flag: Boolean | null): int;
native function with_optional_double(required: double, optional: Double | null): double;
native function with_optional_float(required: float, optional: Float | null): float;
native function with_optional_int(required: int, optional: Int | null): int;
native function with_optional_long(required: long, optional: Long | null): long;
native function with_optional_string(required: string, optional: String | null): string;


loadLibrary("ani_example_optional");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-optional");
__assert_eq_int("with_default_simple", with_default_simple(3, 4), 12);
__assert_eq_int("with_optional_boolean_null", with_optional_boolean(8, null), 8);
__assert_eq_string("with_optional_string", with_optional_string("x", "y"), "x y");

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-optional");
