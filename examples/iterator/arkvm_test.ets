// Auto-generated arkvm smoke test for ani-example-iterator.
// Regenerate with: ./scripts/generate_arkvm_smoke_ets.sh

// Auto-generated by ani-rs at compile time.

export class CountdownIterator implements Iterator<int> {
  current: int = 0;
  native constructor(start: int);
  native $_iterator(): CountdownIterator;
  native __ani_native_next(): int | null;
  next(): IteratorResult<int> {
    let __ani_result = this.__ani_native_next();
    if (__ani_result == null) {
      return new IteratorResult<int>();
    }
    return new IteratorResult<int>(__ani_result);
  }
}

export native function iter_count(values: Iterable<Object>): int;
export native function iter_join_strings(values: Iterable<string>): string;
export native function iter_sum_numbers(values: Iterable<double>): double;
export native function iter_take_strings(values: Iterable<string>, limit: int): Array<string>;

// Public Promise continuation bridge used by PromiseFuture.
export native function __ani_rs_promise_resolve(token: long, value: Object): void;
export native function __ani_rs_promise_reject(token: long, reason: Object): void;
export function __ani_rs_observe_promise<T>(promise: Promise<T>, token: long): void {
promise.then(
(value: T): void => { __ani_rs_promise_resolve(token, value as Object); },
(reason: Object): void => { __ani_rs_promise_reject(token, reason); }
);
}
export native function __ani_rs_cancel_runtime_task(token: long, reason: Object): void;
export class AniCancelHandle {
private token: long;
constructor(token: long) { this.token = token; }
cancel(reason: Object): void { __ani_rs_cancel_runtime_task(this.token, reason); }
}



loadLibrary("ani_example_iterator");

let __ani_fail_count: int = 0;

function __ani_pass(name: string): void {
  console.log("[ASSERT PASS] " + name);
}

function __ani_fail(name: string, detail: string): void {
  console.log("[ASSERT FAIL] " + name + " :: " + detail);
  __ani_fail_count += 1;
}

function __assert_true(name: string, cond: boolean): void {
  if (cond) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "condition=false");
  }
}

function __assert_bool(name: string, got: boolean): void {
  __assert_true(name, got || !got);
}

function __assert_eq_int(name: string, got: int, expected: int): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_long(name: string, got: long, expected: long): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_double(name: string, got: double, expected: double): void {
  let delta = got - expected;
  if (delta < 0.0) {
    delta = -delta;
  }
  if (delta < 0.000001) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_eq_string(name: string, got: string, expected: string): void {
  if (got == expected) {
    __ani_pass(name);
  } else {
    __ani_fail(name, "got=" + got + ", expected=" + expected);
  }
}

function __assert_throws(name: string, fn: () => void): void {
  let thrown = false;
  try {
    fn();
  } catch (_e) {
    thrown = true;
  }
  __assert_true(name, thrown);
}

console.log("[arkvm] smoke start: ani-example-iterator");
let iterWords: Array<string> = ["a", "b", "c"];
__assert_eq_string("iter_join_strings_iterable", iter_join_strings(iterWords), "a,b,c");
__assert_eq_string("iter_join_strings_iterator", iter_join_strings(iterWords.values()), "a,b,c");
let iterWordSet = new Set<string>(0);
iterWordSet.add("x");
iterWordSet.add("y");
__assert_eq_string("iter_join_strings_set", iter_join_strings(iterWordSet), "x,y");
let iterNums: Array<double> = [1.5, 2.5, 3.0];
__assert_eq_double("iter_sum_numbers", iter_sum_numbers(iterNums), 7.0);
let iterTaken = iter_take_strings(iterWords, 2);
__assert_eq_int("iter_take_strings_len", iterTaken.length as int, 2);
__assert_eq_string("iter_take_strings_first", iterTaken[0], "a");
__assert_eq_int("iter_count_array", iter_count(iterWords), 3);

let countdown = new CountdownIterator(3);
let countdownFirst = countdown.next();
__assert_true("countdown_first_not_done", !countdownFirst.done);
__assert_eq_int("countdown_first_value", countdownFirst.value as int, 3);
let countdownTotal: int = 0;
for (const item of new CountdownIterator(3)) {
  countdownTotal += item;
}
__assert_eq_int("countdown_for_of_total", countdownTotal, 6);
__assert_eq_int("iter_count_countdown_roundtrip", iter_count(new CountdownIterator(4)), 4);

if (__ani_fail_count > 0) {
  throw new Error("arkvm assertions failed: " + __ani_fail_count);
}
console.log("[arkvm] smoke done: ani-example-iterator");
